# 根目录临时文件
/repos.json
/repo_detail.json

# 各仓库的 star 用户原始缓存现在需要用于时间图表，不再忽略

# Claude 工作目录
.claude/

# 内部开发者名单（敏感数据，通过 CI Secret / 私有仓注入）
config/internal_developers.txt

# GitCode 用户名→邮箱映射（敏感数据，通过 CI Secret / 私有仓注入）
config/gitcode_2_mail.txt

# SMTP 发送配置（敏感数据，通过 CI Secret / 私有仓注入）
config/smtp_config.ini

# 管理员邮箱（敏感数据，通过 CI Secret / 私有仓注入）
config/admin_email.txt

# Python 缓存
__pycache__/
